export enum Code {
  SendMessage,
  ConfirmationAccepted,
  ConfirmationRejected,
}

export enum Action {
  Ready,
}

export enum Status {
  Ok,
}

export enum Confirmation {
  Accepted,
  Rejected,
}
